services:
  app_proxy:
    environment:
      APP_HOST: sparkles-shopping-list_web_1
      APP_PORT: 3000

  web:
    image: gbletsch/shopping-list:latest
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/data
